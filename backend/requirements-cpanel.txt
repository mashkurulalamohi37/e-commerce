# Runtime dependencies for the Namecheap cPanel (Passenger) deployment.
#
# Two differences from requirements.txt, both deliberate:
#
#  1. No asyncpg. Namecheap shared hosting provides MySQL/MariaDB only -- there
#     is no PostgreSQL to connect to -- so the driver is dead weight, and a
#     source build of it on a shared host with no compiler is a common cause of
#     a failed `pip install` there.
#  2. No pytest/httpx/asgi-lifespan. Test-only, and every extra package is more
#     of the plan's inode and disk quota.
#
# This file duplicates version pins from requirements.txt and will drift from it.
# When you change a pin there, change it here too.

fastapi>=0.110.0,<0.116.0
uvicorn[standard]>=0.28.0,<0.35.0
sqlalchemy[asyncio]>=2.0.28,<2.1.0
aiosqlite>=0.20.0
alembic>=1.13.1
pydantic>=2.6.4
pydantic-settings>=2.2.1
pyjwt>=2.8.0
bcrypt>=4.1.0,<5.0.0
python-multipart>=0.0.9
slowapi>=0.1.9
fastapi-mail>=1.4.1
a2wsgi>=1.10.0
python-dotenv>=1.0.1
