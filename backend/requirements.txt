fastapi>=0.110.0,<0.116.0
uvicorn[standard]>=0.28.0,<0.35.0
sqlalchemy[asyncio]>=2.0.28,<2.1.0
asyncpg>=0.29.0
alembic>=1.13.1
pydantic>=2.6.4
pydantic-settings>=2.2.1
pyjwt>=2.8.0
# bcrypt directly, not passlib: passlib 1.7.4 (last release 2020) reads
# bcrypt.__about__.__version__, which bcrypt 4.x removed, so every hash logged a
# trapped AttributeError. Hashes are the same $2b$ format, so existing passwords
# still verify.
bcrypt>=4.1.0,<5.0.0
python-multipart>=0.0.9
slowapi>=0.1.9
fastapi-mail>=1.4.1
a2wsgi>=1.10.0
python-dotenv>=1.0.1
pytest>=8.1.1
pytest-asyncio>=0.23.0
asgi-lifespan>=2.1.0
httpx>=0.27.0
